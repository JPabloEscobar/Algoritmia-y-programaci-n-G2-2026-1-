"""
Implementar los siguientes ejercicios durante el tiempo de clase:

1. Escribe una función llamada factorial(n) que calcule el producto 
de todos los números enteros positivos desde 1 hasta n.

2. En lugar de usar filter y map, crearemos una función que recorra
una lista, decida si un número es par y, si lo es, lo eleve al cuadrado,
todo en un solo paso recursivo.

3. Crea una función llamada suma_recursiva(lista) que reciba una lista de
números y devuelva la suma total de sus elementos.

4. Escribe una función recursiva llamada invertir_cadena(texto) que reciba
un string y lo devuelva invertido.

5. Este ejercicio sustituye la lógica de un acumulador (reduce) o la función
max(). Queremos encontrar el número más grande comparando elementos uno a uno.
"""

####New-Item nombre del archivo(.py) #Es para crear un archivo desde el power shell, no es parte del código a escribir en el ejercicio